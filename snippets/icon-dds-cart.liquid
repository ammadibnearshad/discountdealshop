{% comment %}
  DDS cart icon — a bold rounded shopping bag, replacing Dawn's hairline
  40x40 cart glyph on every surface that renders the cart button.

  Carries the `icon` class as well so Dawn's own header rules
  (`.header__icon .icon`) keep applying; `.dds-icon--cart` is what the DDS
  header CSS sizes and weights.

  Decorative — every place it is used pairs it with visually hidden text.
{% endcomment %}
<svg
  class="icon dds-icon dds-icon--cart"
  viewBox="0 0 24 24"
  fill="none"
  aria-hidden="true"
  focusable="false"
><path d="M8 7.5V6.6a4 3.4 0 0 1 8 0v.9"/><path d="M4 9.5A2 2 0 0 1 6 7.5h12a2 2 0 0 1 2 2v8.6A2.9 2.9 0 0 1 17.1 21H6.9A2.9 2.9 0 0 1 4 18.1V9.5Z"/></svg>
